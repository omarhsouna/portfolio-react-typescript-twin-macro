/** @jsxImportSource @emotion/react */
import tw from "twin.macro";
import styled from "@emotion/styled";

const Field = styled.input(() => [tw`w-full px-5 rounded-[5px] h-[50px] text-base border-2 border-borderInput bg-transparent text-borderInput outline-none`]);

export default Field;
